Original course labs (Modules 1–3) are preserved in [`archive/`](archive/).

The production system is the `src/pdr` package: multimodal RAG, LangGraph agents, MCP tools, and the Gradio UI documented in the root README.
